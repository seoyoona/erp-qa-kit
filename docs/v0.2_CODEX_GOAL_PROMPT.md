# Codex /Goal Prompt — ERP QA Kit v0.2 (paste into Codex)

> Copy everything in the code block below into Codex `/Goal`.

```
Read docs/v0.2_PLAN.md FIRST, then follow this reading order:
PLAN → v0.2_CONSTRAINTS → v0.2_ARCHITECTURE → v0.2_PROJECT_POLICY_SPEC →
v0.2_PROJECT_MEMORY_SPEC → v0.2_EXTRACTOR_STRATEGY → v0.2_SPEC →
v0.2_ACCEPTANCE_CRITERIA → v0.2_DRY_RUN_GUIDE. These v0.2 docs are the single source
of truth; the v0.1 docs (INDEX.md, SPEC.md, CONSTRAINTS.md, ARCHITECTURE.md) remain
binding for all inherited behavior.

Goal: implement ERP QA Kit v0.2 — project policy + persistent project/module memory +
module-level screen-contract validation + executable file-extractor adapters — runnable
on a module-scoped extracted folder, WITHOUT breaking v0.1.

CLI (do not re-specify; build per the docs): granular flat hyphenated subcommands
(policy-init, module-init, extract-spec, extract-frontend, extract-backend,
extract-procedure, compare-contract, module-report, module-handoff) PLUS an orchestrated
`module-audit`. Module commands take project_path + --module.

Extractors must be REAL — wired to the CLI, covered by tests and fixtures, and documented;
NOT placeholder folders. Required: xlsx_text_adapter, xlsx_image_manifest_adapter (openpyxl;
image manifest, NO OCR), csv_adapter, markdown_adapter, json_yaml_adapter, frontend_static_adapter,
backend_static_adapter, procedure_sql_adapter. Optional extras (pandas, MarkItDown) must be
graceful-if-absent. Docling/Unstructured are future-only — do NOT implement. openpyxl is the
ONLY new required dependency.

Hard limits (override convenience): never execute a DB; never modify target ERP code; the
frontend is the implementation under test, NOT a source of truth, and frontend override is
forbidden; OCR/image-derived text is low/medium confidence with needs_human_confirmation
true (never high); module-scope only — no full-repo analysis and no cloning all repos;
provenance on every inferred item; do not break v0.1.

Do NOT invent requirements, features, dependencies, or tool choices beyond these docs, and
do NOT search the web for tooling decisions.

Follow the build order in docs/v0.2_PLAN.md and self-verify against
docs/v0.2_ACCEPTANCE_CRITERIA.md. When done, confirm a fixture module runs `module-audit`
end-to-end and report which acceptance sections pass.
```
